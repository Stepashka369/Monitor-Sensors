databaseChangeLog:
  - changeSet:
      id: create-user-data-type
      author: stepan
      changes:
        - sql:
            dbms: postgresql
            sql: CREATE TYPE unit_enum AS ENUM ('bar', 'voltage', '°C', '%');
        - sql:
            dbms: postgresql
            sql: CREATE TYPE type_enum AS ENUM ('pressure', 'voltage', 'temperature', 'humidity');
